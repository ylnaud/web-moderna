---
layout: post.njk
pagination:
  data: collections.tags
  size: 1
  alias: tag
permalink: /tags/{{ tag | slug }}/
---

<h2>Etiquetado “{{ tag }}”</h2>
<ol>
  {% set taglist = collections.tags[tag] %}
  {% if taglist %}
    {% for post in taglist | reverse %}
      <li>
        <a href="{{ post.url }}">{{ post.data.title }}</a>
      </li>
    {% endfor %}
  {% else %}
    <p>No hay publicaciones para esta etiqueta.</p>
  {% endif %}
</ol>