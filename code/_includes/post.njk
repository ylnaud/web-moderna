<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>{{ title }}</title>
    <link rel="icon" href="/img/logo.png"/>
    {% googleFontsInline 'https://fonts.googleapis.com/css?family=Host+Grotesk:400&display=swap' %}
    <meta name="description" content="{{ descripcion }}"/> {% set css %}
    {% include "styles.css" %}
    {% endset %} <link rel="stylesheet" href="css/normalize.css"/>
    <link
    rel="stylesheet" href="css/critico.css"/>
    <!-- feed it through our cssmin filter to minify -->
    <style>
      {{css | cssmin | safe}}</style>
  </head>
  <body>
    <div id="skip">
      <a class="skip-link" href="#content">Saltar al contenido principal</a>
    </div>
    <section class="container">
      {% include "layouts/ui/menu.njk" %}
    </section>
    <main id="content">{{ content | safe }}</main>
    {# {% include "footer.njk" %} #}
    <!-- feed it through our jsmin filter to minify -->
    {% set js %}
    {% include "index.js" %}
    {% endset %}
    <!-- feed it through our jsmin filter to minify -->
    <script>
      {{ js | jsmin | safe }}</script>
    <script type="module" src="app/index.js"></script>
  </body>
</html>